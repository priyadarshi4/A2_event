<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>A2_Restaurant</title>
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&family=Playfair+Display:wght@400;600&display=swap" rel="stylesheet">

    <style>
        /* Global Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background: #212121;
            color: #f5f5f5;
            line-height: 1.6;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Playfair Display', serif;
            font-weight: 600;
        }

        /* Header Section */
        header {
            position: relative;
            background: url('https://img.freepik.com/free-photo/top-view-table-full-delicious-food-composition_23-2149141353.jpg') center center no-repeat;
            background-size: cover;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: white;
            padding: 0 20px;
            box-shadow: inset 0 0 80px rgba(0, 0, 0, 0.6);
        }

        header h1 {
            font-size: 4.5rem;
            text-transform: uppercase;
            letter-spacing: 8px;
            margin-bottom: 20px;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.7);
        }

        header p {
            font-size: 1.5rem;
            opacity: 0.8;
            margin-bottom: 30px;
            letter-spacing: 1px;
        }

        header .cta-button {
            background-color: #f5a623;
            padding: 18px 60px;
            color: white;
            font-size: 1.2rem;
            font-weight: 500;
            border-radius: 50px;
            letter-spacing: 2px;
            text-transform: uppercase;
            transition: all 0.3s ease;
        }

        header .cta-button:hover {
            background-color: #ff7f00;
            transform: translateY(-5px);
        }

        /* About Section */
        .about {
            background: #333333;
            padding: 100px 20px;
            text-align: center;
        }

        .about h2 {
            font-size: 3.5rem;
            margin-bottom: 30px;
        }

        .about p {
            font-size: 1.25rem;
            opacity: 0.9;
            max-width: 900px;
            margin: 0 auto;
            line-height: 1.8;
        }

        /* Menu Section */
        .menu {
            padding: 80px 20px;
            text-align: center;
            background: #222222;
        }

        .menu h2 {
            font-size: 3.5rem;
            color: #f5a623;
            letter-spacing: 2px;
            margin-bottom: 50px;
        }

        .menu .menu-items {
            display: flex;
            justify-content: center;
            gap: 30px;
            flex-wrap: wrap;
        }

        .menu .menu-item {
            background: #444444;
            padding: 30px;
            border-radius: 10px;
            width: 300px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            text-align: left;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            overflow: hidden;
        }

        .menu .menu-item:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.7);
        }

        .menu .menu-item img {
            width: 100%;
            height: 200px;
            object-fit: cover;
            border-radius: 10px;
            margin-bottom: 20px;
        }

        .menu .menu-item h3 {
            font-size: 2.2rem;
            color: #f5a623;
            margin-bottom: 15px;
        }

        .menu .menu-item p {
            font-size: 1.2rem;
            opacity: 0.8;
            margin-bottom: 20px;
        }

        .menu .menu-item .price {
            font-size: 1.4rem;
            font-weight: bold;
            color: #f5a623;
        }

        /* Footer Section */
        footer {
            background: #333333;
            color: #f5f5f5;
            padding: 50px 20px;
            text-align: center;
        }

        footer p {
            font-size: 1.2rem;
            opacity: 0.8;
        }

        /* Responsive Design */
        @media (max-width: 1024px) {
            .menu .menu-item {
                width: calc(50% - 40px);
            }

            header h1 {
                font-size: 4rem;
            }

            header p {
                font-size: 1.3rem;
            }

            header .cta-button {
                font-size: 1rem;
                padding: 12px 40px;
            }

            .about p {
                font-size: 1.1rem;
            }

            .menu .menu-item h3 {
                font-size: 2rem;
            }
        }

        @media (max-width: 768px) {
            .menu .menu-item {
                width: 100%;
            }

            header h1 {
                font-size: 3.5rem;
            }

            header .cta-button {
                font-size: 1rem;
                padding: 12px 30px;
            }

            .about p {
                font-size: 1rem;
            }

            .menu .menu-item h3 {
                font-size: 1.8rem;
            }
        }

        @media (max-width: 480px) {
            header h1 {
                font-size: 3rem;
            }

            header .cta-button {
                font-size: 0.9rem;
                padding: 10px 25px;
            }

            .about p {
                font-size: 1rem;
            }

            .menu .menu-item h3 {
                font-size: 1.6rem;
            }
        }
    </style>
</head>
<body>

    <!-- Header Section -->
    <header>
        <div>
            <h1>A2_Restaurant</h1>
            <p>Where Exquisite Dining Meets Timeless Elegance</p>
            <a href="#menu" class="cta-button">Explore Our Menu</a>
        </div>
    </header>

    <!-- About Section -->
    <section class="about" id="about">
        <h2>About Us</h2>
        <p>At A2_Restaurant, we aim to deliver a refined dining experience like no other. With a team of culinary experts and a passion for high-quality ingredients, we create dishes that not only satisfy but exceed expectations. From our chic industrial ambiance to our meticulously crafted menu, everything at A2_Restaurant is designed to offer a memorable and sophisticated experience.</p>
    </section>

    <!-- Menu Section -->
    <section class="menu" id="menu">
        <h2>Our Menu</h2>
        <div class="menu-items">

            <% events.forEach(function(event) { %>
                <div class="menu-item">
                    <img src="data:image/png;base64, <%= event.eventLogo.toString('base64') %>" alt="<%= event.eventLogo %>">
                    <h3><%= event.name %></h3>
                    <p>Price: $<%= event.prizeMoney %></p>
                </div>
            <% }) %>
            
        </div>
    </section>

    <!-- Footer Section -->
    <footer>
        <p>© 2024 A2_Restaurant | All Rights Reserved</p>
    </footer>

</body>
</html>
