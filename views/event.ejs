<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event Management</title>
    <style>
        /* Basic Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background: #121212;
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            flex-direction: column;
        }

        /* Container for Links */
        .link-container {
            background: #333;
            border-radius: 15px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5);
            padding: 40px 30px;
            text-align: center;
            width: 400px;
            animation: fadeIn 1.5s ease-in-out;
        }

        /* Header Styling */
        .link-container h2 {
            font-size: 2.5rem;
            color: #f5a623;
            margin-bottom: 20px;
        }

        /* Link Styling */
        .link-container a {
            display: block;
            padding: 15px;
            margin: 15px 0;
            text-decoration: none;
            color: #fff;
            background: #f5a623;
            border-radius: 10px;
            font-size: 1.2rem;
            transition: background 0.3s ease, transform 0.3s ease;
        }

        .link-container a:hover {
            background: #ff7f00;
            transform: translateY(-5px);
        }

        /* Logout Button Styling */
        .link-container a:last-child {
            background: #444;
            color: #f5a623;
            margin-top: 20px;
        }

        .link-container a:last-child:hover {
            background: #555;
            color: #ff7f00;
        }

        /* Fade Animation */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="link-container">
        <h2>Admin Management</h2>
        <a href="/admin/event">Add menu</a>
        <a href="/admin/allevent">All meanu</a>
        <a href="/admin/addartist">Add Artist</a>
        <a href="/admin/addsong">Add Song</a>
        <a href="/admin/logout">Logout</a>
    </div>
</body>
</html>
