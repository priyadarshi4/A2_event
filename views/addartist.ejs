<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Artist</title>
    <style>
        /* Basic Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background: #121212;
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            flex-direction: column;
        }

        /* Form Container */
        .form-container {
            background: #333;
            padding: 40px 30px;
            border-radius: 15px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5);
            width: 400px;
            animation: fadeIn 1.5s ease-in-out;
        }

        .form-container h1 {
            text-align: center;
            color: #f5a623;
            margin-bottom: 20px;
            font-size: 2.5rem;
        }

        /* Form Fields */
        .form-container label {
            font-size: 1.2rem;
            color: #f5a623;
            display: block;
            margin-bottom: 10px;
        }

        .form-container input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #555;
            border-radius: 8px;
            background: #444;
            color: #fff;
            font-size: 1rem;
            outline: none;
            transition: border-color 0.3s ease;
        }

        .form-container input[type="text"]:focus {
            border-color: #f5a623;
        }

        /* Button Styling */
        .form-container button {
            width: 100%;
            padding: 10px;
            background: #f5a623;
            border: none;
            border-radius: 8px;
            color: #fff;
            font-size: 1.2rem;
            cursor: pointer;
            transition: background 0.3s ease, transform 0.3s ease;
        }

        .form-container button:hover {
            background: #ff7f00;
            transform: translateY(-2px);
        }

        /* Fade Animation */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h1>Add Artist</h1>
        <form action="/admin/addartist" method="POST">
            <label for="name">Artist Name:</label>
            <input type="text" name="name" id="name" placeholder="Enter artist name" required>
            
            <label for="image">Artist Image URL:</label>
            <input type="text" name="image" id="image" placeholder="Enter artist image URL" required>
            
            <button type="submit">Add Artist</button>
        </form>
    </div>
</body>
</html>
